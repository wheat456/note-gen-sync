# 设置代理
```
[http] proxy = http://proxy.example.com:8080 
[https] proxy = http://proxy.example.com:8080
```
### 命令行
- `git config http.proxy http://127.0.0.1:44550`
# 设置ssh key
```
[core]
sshCommand = ssh -i c:/df/fsd/fsd
```
# 配置用户
```
[user]
    name = Your Name
    email = your.email@example.com
```
