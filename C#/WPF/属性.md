
PART_ 是一个特定的名字，可以让后端代码找到对应的组件
仅存在于 **`ControlTemplate`（控件模板）** 内部，`x:Name=PART_ContentHost`

IsTabStop="False" 按下tab时，无法聚焦到这个组件
# 使用Windows的符号
`FontFamily="Segoe MDL2 Assets"` 可以使用**window**的符号库 比如`text="&#xE70D;"`
