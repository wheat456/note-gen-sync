
- **DependencyObject**: 能绑定数据吗？
    
- **Visual**: 能在屏幕显示吗？
    
- **UIElement**: 能接收鼠标点击吗？
    
- **FrameworkElement**: 能设置宽度和对齐吗？
    
- **Control**: 有模板可以换肤吗？
    
- **Content/Items**: 是放一个东西还是放一堆东西？



# 可以使用的控件的继承

## 1.`ContentControl` 类
- 其特点是 **“只能有一个孩子”**
- **核心属性**：`Content`
- 属性`Template` 控件自己长什么样  类型：（`ControlTemplate`）
	- `Template` 里面必须放 `ContentPresenter`
- 属性`ContentTemplate` 内容长什么样   类型：（`DataTemplate`）
	- 会自动将父类的`ContentTemplate`传递给`ContentPresenter`中的`ContentTemplate` 属性
	- `ContentPresenter` 会自动去读取 `Content + ContentTemplate` 并显示
### 2. ContentPresenter 是什么？
它是**独立的 FrameworkElement**，不是控件，**只是一个呈现容器**。
它内部也复刻了：
- Content
- ContentTemplate
- ContentTemplateSelector
但**设计目的不是让你手动赋值**，是用来**自动继承上层 ContentControl 的值**。
## 2. `ItemsControl` 类
这类控件用于 **“显示一组数据”**
**核心属性**：`ItemsSource`
- 有`template`属性，用于定义自己的样子
	- 必须放`ItemsPresenter`

## 3.`Panel`类
这类组件不继承自 `Control`，而是直接继承自 `FrameworkElement` 下的 `Panel` 类
它们没有 `ControlTemplate`

## 4.文本与基础装饰类 (Text & Decorator)
**文本类**：`TextBlock`。它甚至不继承自 `Control`（所以没有模板），是为了性能而设计的极简文本渲染组件。

**装饰器类 (Decorator)**：继承自 `Decorator`。它们通常包裹一个组件并为其添加视觉效果。

- `Border`: 给内容加边框和背景。
- `Viewbox`: 自动缩放内部内容。


| 类别    | 基类               | 内容属性        | 典型用法          |
| ----- | ---------------- | ----------- | ------------- |
| 内容控件  | ContentControl   | Content     | 按钮、窗口（承载单一主体） |
| 集合控件  | ItemsControl     | ItemsSource | 列表、下拉框、树形菜单   |
| 布局面板  | Panel            | Children    | 网格、堆栈（负责排版）   |
| 文本/装饰 | FrameworkElement | Text/Child  | 文字显示、加边框、缩放   |


# 模板
WPF 只有**两种核心模板**

1. **控件模板 `ControlTemplate`**：给**控件本身**用（按钮边框、样式、内部结构）
2. **数据模板 `DataTemplate`**：给**数据**用（列表里每一项显示什么、怎么布局）

# 呈现器
- `ContentPresenter`：呈现**内容控件**的内容
- `ItemsPresenter`：呈现**列表控件**的子项




