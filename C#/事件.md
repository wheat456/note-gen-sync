```c#
public class Bot
{
    // 使用内置的 Action 委托
    public event Action<string> OnMessage; //这样不用定义委托了
    
    public void ReceiveMessage(string msg)
    {
        OnMessage?.Invoke(msg);
    }
}
```