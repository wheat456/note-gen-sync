```cs
using System.IO;

// --- 判断文件夹是否存在 ---
if (Directory.Exists(@"C:\MyFolder"))
{
    // 文件夹存在
}

// --- 判断文件是否存在 ---
if (File.Exists(@"C:\MyFolder\config.json"))
{
    // 文件存在
}
```

`[ComVisible(true)]` 在一个类上标记

webView.CoreWebView2.AddHostObjectToScript("myBridge", new Bridge());

const bridge = window.chrome.webview.hostObjects.myBridge;

await bridge.ShowMessage("Hello WPF!");


const result = await bridge.GetDataFromBackend("测试参数");

string[] files = Directory.GetFiles(configFilesPath, "*.json");
["C:\Data\config.json", "C:\Data\settings.json"]