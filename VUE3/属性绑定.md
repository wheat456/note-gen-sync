在 Vue 中，mustache 语法 (即双大括号) 只能用于==文本插值==。为了给 attribute 绑定一个动态值，需要使用 `v-bind` 指令：
```js
<div v-bind:id="dynamicId"></div> //id是这个组件的参数
简写
<div :id="dynamicId"></div>
```

其中`dynamicId`是一个变量，不需要写`{}`,并且再模板中不需要写`.value`
