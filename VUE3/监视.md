```ts
const stopwatch=watch(谁？,(新的值，旧的值)=>{}){
}
stopwatch() //结束监视
```
如果监视对象，则监视的是对象地址
```js
const person = {
	name:"nihao",
	age:18
}
watch(person,(new,old)=>{
	
})
```
当整个person 变化时，才会触发监视，如果需要监控字段，则
```js
watch(...,{deep:true，immediate:true})
```
deep:深度监视，字段变化会触发监视
immediate:启动时直接执行一次


> [!NOTE] Title
> Contents
