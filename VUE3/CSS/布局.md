## **Grid（网格布局**）


**inline-flex**：默认的flex会在当前标签的下一个标签换行，如果使用了这个，下一个标签就会紧贴当前标签
**block**：独占一行
**inline-block**：不换行了，但可以设置宽高，inline不能设置宽高
下面的内容都是给子元素使用的，在父元素上定义flex,子元素来定义下面的内容
**flex-1**：填满剩余的空间，flex-2所占的空间是flex-1的两倍
**flex-none**：**完全死板**，既不变大也不变小。
**flex-auto**：**能占满空间就占满**，不够了就缩小。
**flex-initial**：**只占自己内容的宽度**，绝不变大，但空间不够时会缩小。
**flex-grow**：当父容器有剩余空间时，这个元素会努力去**占满**这些空间。
**flex-grow-0**：不管父容器有多少剩余空间，这个元素都**只占自己内容那么大**，绝不多占。
**box-border**： 设置宽高是会包含border的宽度，比如设置的是100实际上内容只占80，不包含margin，但包含padding
**solid**：指的是实线

| 参数 | 作用 | 简单理解 |
| :--- | :--- | :--- |
| **`static`** (默认) | **普通文档流**，不能使用 `top`/`left` 等定位。 | **老实排队**，怎么写怎么排，不搞特殊。 |
| **`relative`** (相对定位) | **相对于自己原来的位置**进行偏移。 | **在原位基础上挪一下**，但不占坑（后面元素当它还在原位）。 |
| **`absolute`** (绝对定位) | **相对于最近的非 `static` 祖先元素**进行定位。 | **脱离文档流**，飘起来，随便找“有地位的”祖先当参照物。 |
| **`fixed`** (固定定位) | **相对于浏览器窗口**进行定位，滚动时不随页面移动。 | **钉在屏幕上**，像“回到顶部”按钮，怎么滚都在那。 |
| **`sticky`** (粘性定位) | 基于用户的滚动位置在 `relative` 和 `fixed` 之间切换。 | **滑到某个位置就吸住**，比如表头、导航栏。 |

flex-1  自动填满父容器剩余的空间
flex-5 是其他的5倍


`<base>` 放在head中，为其他herf提供基本路径


code 就是和markdown的code一样
data 人类看到的是文本，背后可以设置数值
配合使用
table
- colgroup 
	- col


input  `list='id'`
- datalist `datalist id= xxx`
	- option `option value="sss"`

- **`<dl>` (Description List)**：**容器**。告诉浏览器：“接下来的内容都是一组组的术语和解释”。
    - **`<dt>` (Description Term)**：**术语/名字**。你要解释的那个词。
    - **`<dd>` (Description Details)**：**描述/解释**。具体的解释内容。




- **`<details>`**：**容器**。它负责控制内容的显示与隐藏状态。
    - **`<summary>`**：**按钮/标题**。它是用户点击的区域。如果不写它，浏览器会默认显示“详细信息”（或 "Details"）作为标题。
`<dfn>`定义一个术语

dialog定义对话框

embed嵌入pdf


form 
- fieldset 
	- fieldset 相当于一个标题

figure
- `<figcaption>` 提供文字说明

footer 需要配合文本标签使用
header 作用和footer差不多
`<hgroup>` 里面定义一些标题
`hr`水平分割线
`iframe`嵌入网页


ul 配合起来是是一个无序列表
- li
menu 
- li
link 放在head中


map 配合使用
- area 
meter 像一个温度计一样的组件

nav
- ol有序列表
- li 无序列表
object 可以嵌入图片音频视频


video
- track 用于添加字幕
# 文章
`i`标签斜体
`b`加粗
`br` 换行
`del` 划线
`ins` 配合del使用，表示是新插入的文本
`em`强调
`kbd` 描述键盘上的符号
`mark` 高亮
`wbr`一个单词或长字符串，但如果空间不够，你可以在这里换行
`var` 表示数学或者代码中变量
`u` 下划线

# 组件
table
- `<caption>` 主要用于为表格添加**标题**或**简要说明**,比如表格下方的注解
- `tr`表格行
	- th就是该行的第一个
	- td就是内容格
