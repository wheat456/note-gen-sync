
# stackpanel
- `spacing` 间距

# grid
- `ColumnSpacing` 
- `RowSpacing`

# relativepanel
## 用在这个容器中的组件上
- **`RelativePanel.AlignTopWithPanel="True"`**: 与 Panel 顶部对齐
- **`RelativePanel.AlignBottomWithPanel="True"`**: 与 Panel 底部对齐
- **`RelativePanel.AlignLeftWithPanel="True"`**: 与 Panel 左侧对齐
- **`RelativePanel.AlignRightWithPanel="True"`**: 与 Panel 右侧对齐
- **`RelativePanel.AlignHorizontalCenterWithPanel="True"`**: 在 Panel 中水平居中
- **`RelativePanel.AlignVerticalCenterWithPanel="True"`**: 在 Panel 中垂直居中
# 用在这个容器中的组件同级
- **`RelativePanel.RightOf="TargetName"`**: 位于 `TargetName` 的**右侧**
- **`RelativePanel.LeftOf="TargetName"`**: 位于 `TargetName` 的**左侧**
- **`RelativePanel.Below="TargetName"`**: 位于 `TargetName` 的**下方**
- **`RelativePanel.Above="TargetName"`**: 位于 `TargetName` 的**上方**

##  同级元素对齐
- **`RelativePanel.AlignTopWith="TargetName"`**: 与 `TargetName` **顶边对齐**
- **`RelativePanel.AlignBottomWith="TargetName"`**: 与 `TargetName` **底边对齐**
- **`RelativePanel.AlignLeftWith="TargetName"`**: 与 `TargetName` **左边对齐**
- **`RelativePanel.AlignRightWith="TargetName"`**: 与 `TargetName` **右边对齐**
- **`RelativePanel.AlignHorizontalCenterWith="TargetName"`**: 与 `TargetName` **水平中心对齐**
- **`RelativePanel.AlignVerticalCenterWith="TargetName"`**: 与 `TargetName` **垂直中心对齐**


# button 
- `ClickMode` 
- `flyout` 出现悬浮菜单
# dropdownbutton

- 同button

# textbox
- `PlaceholderText` 空值时的提示文字（灰字提示）
- `Header` 输入框上方的标题/说明，可放文字或任意 UI
- `Description` 输入框下方的辅助说明文字
- `IsReadOnly` 只读，可复制但不可编辑
- `AcceptsReturn​` 是否允许回车换行（false 时回车会丢失焦点）
- `TextWrapping​` 内容超出宽度时是否自动换行
- `MaxLength (int)` 最大可输入字符数，0 表示不限制


# checkbox 
- `IsThreeState` 是否启用三态模式
- 

# radiobutton 
- `GroupName` 分组名，同一容器内同名 RadioButton 自动互斥；不指定时默认按同一父容器自动分组

# togglebutton 


# image
- `Stretch`
	- `None`
	- `Fill`
	- `Uniform`
	- `UniformToFill`
- `Source=` 指定文件路径

# Rectangle
- `RadiusX / RadiusY` 设置沿 X 轴和 Y 轴的圆角半径，用于实现圆角矩形
- `StrokeDashArray` 设置虚线边框样式，通过数字集合指定实线与空白的交替长度（如 `"4 2"`）。
- `StrokeLineJoin` 控制虚线端点样式及线条拐角连接处的形状（如 `Miter`、`Bevel`、`Round`）。
# line
- **StrokeStartLineCap / StrokeEndLineCap**：控制线条起点和终点的端点形状（如 `Flat`、`Square`、`Round`、`Triangle`）。
    
- **StrokeDashCap**：控制虚线中各个虚线段端点的形状。

# InfoBar
- `IsOpen` 是否显示
- `Title` 
- `Message`
- `Severity` 设置提示的严重程度，会自动调整控件的图标和背景颜色。
	- `Informational`
	- `Success`
	- `Warning`
	- `Error`
- `IsClosable` 设置是否在右侧显示关闭按钮
- `ActionButton` 可传入一个控件（如 `Button`），作为用户点击采取行动的操作按钮
- `Content` 
- `IconSource` 自定义提示栏左侧显示的图标，覆盖 `Severity` 默认的系统图标

# teachingtip
- `IsOpen` 控制提示框的打开或关闭状态
- `Target` 
- **Title**：设置提示框的标题文本。
- **Subtitle**：设置标题下方的副标题/详细描述文本。
- **Content**：自定义内容区域，可嵌入任意复杂控件（如 `Image`、`StackPanel`）。
- **HeroContent**：设置位于提示框顶部的突出展示内容（通常用于展示引导图片或视频）。
- **IconSource**：设置标题旁的图标样式。
- **ActionButtonContent**：主要操作按钮的文本或内容（如“知道了”）。
- **CloseButtonContent**：关闭按钮的文本（默认显示关闭图标或自定义文本）。
- **IsLightDismissEnabled**：设置是否开启轻点关闭模式（即点击提示框外部区域时自动关闭）。
- - **PreferredPlacement**：设置提示框相对于 `Target` 的首选定位方向（如 `Bottom`、`TopRight`、`Auto` 等）。
- **PlacementMargin**：控制提示框与其目标元素之间的边距。


