## 基本类型

- **Byte**：1 字节。范围 0 到 255。无符号。
- **Integer**：2 字节。范围 -32,768 到 32,767。整数。
- **Long**：4 字节。范围约正负 21 亿。长整数。
- **Single**：4 字节。单精度浮点数。处理小数。
- **Double**：8 字节。双精度浮点数。高精度小数。
- **Currency**：8 字节。定点货币型。适合财务计算，避免四舍五入误差。

- **String（变长）**：长度随内容变化。最多可容纳约 20 亿个字符。
- **String（定长）**：声明时指定长度。形如 `Dim Str As String * 10`。
- **Boolean**：2 字节。仅存储 `True` 或 `False`。
- **Date**：8 字节。存储日期和时间。范围从公元 100 年到 9999 年。

```vb
Dim 变量名 as Integer = 100
变量名=200
```


## 数组
```vb
dim arr(1 to 10) as integer
```

## 常量
```vb
Const 变量名=123
Const xx as xx = 123
```
## 枚举
```vb
Enum Colors
   red = 1
   orange = 2
   yellow = 3
   green = 4
   azure = 5
   blue = 6
   violet = 7
End Enum
```

## 结构体
```vb
Structure Box
Public length As Double           
Public breadth As Double   
Public height As Double
	End Structures
```
## 函数声明
```vb 
Function myFunction
(n As Integer) As Double 
    Return 5.87 * n
End Function
```

## 子语句
- 没有返回的函数，主要用于实现动作
```vb 
Sub mySub(ByVal s As String)
    Return
End Sub
```

# 判断
```vb 
       If x=10 Then

            Console.WriteLine(10)

        Else

            Console.WriteLine(x)

        End If
```

# 循环
```vb

For i As Integer = 1 To 10
    Console.WriteLine(i)
Next

```

## while
```vb 
While condition
    ' 循环体
End While
```

# Sub

- **按值传递** ：此方式下，参数是原值的一个副本，原始数据在传递过程中不会被改变，适用于不需要修改参数值的情况。
- **按引用传递** ：此方式下，传递的是参数的内存地址，这样在 `Sub` 过程中对参数的任何修改都会影响到原始数据。
```vb
Sub ModifyValue(ByVal myValue As Integer, ByRef myRefValue As String)    //byval值传递 byref 引用传递
myValue = myValue + 10    
myRefValue = myRefValue.ToUpper()
End Sub

ModifyValue(value, refValue)

```


# 函数
```vb

Function AddNumbers(ByVal number1 As Integer, ByVal number2 As Integer) As Integer    Return number1 + number2End Function


```

# 对象
```vb
Dim btnMyButton As Button
btnMyButton = New Button

Dim btnMyButton As New Button

btnMyButton.Font = New Font("Arial", 12)

```

