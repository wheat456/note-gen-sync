## 所有权的三条核心规则
- **每个值都有一个所有者（Owner）**：即绑定该值的变量。
- **同一时刻，一个值只能有一个所有者**。
- **当所有者离开其作用域（Scope）时，该值会被自动销毁并释放内存（调用 `Drop`）**。

## 所有权的转移与复制

### 移动
- 对于分配在堆上的复杂数据（如 `String`、`Vec`），赋值或传参时会触发**所有权转移（Move）**

### 复制
- 存储在栈上的基本类型（如整数、浮点数、布尔值、定长数组）实现了 `Copy` Trait，赋值时会自动进行低成本的按位复制，原变量依然有效：

## 借用
| **引用类型**  | **语法**   | **规则与约束**                             |
| --------- | -------- | ------------------------------------- |
| **不可变引用** | `&T`     | 可以同时存在**多个**只读引用。                     |
| **可变引用**  | `&mut T` | 在同一作用域内，**只能存在一个**可变引用，且此时不能有任何不可变引用。 |

## 变量遮蔽
- 当使用同名变量会`shadow`之前的变量,前后两个变量不是同一个，被`shadow`后前一个不能用
```rust
变量遮蔽(shadowing)
fn main() {
    let x = 5;
    // 在main函数的作用域内对之前的x进行遮蔽
    let x = x + 1; //x变量只作用于右边，左边被shadowing

    {
        // 在当前的花括号作用域内，对之前的x进行遮蔽
        let x = x * 2;
        println!("The value of x in the inner scope is: {}", x);
    }

    println!("The value of x is: {}", x);
}
```
## 类型转换
```rust
let decimal=65.2345;
let integer = decimal as u8;//类型转换
```
## 原子类型
```rust
use std::sync::atomic::{AtomicUsize,Ordering};  
static REQUEST_RECV:AtomicUsize=AtomicUsize::new(0);  
  
fn main() {  
    for _ in 0..10{  
        REQUEST_RECV.fetch_add(1,Ordering::Relaxed);  
    }  
}
```
