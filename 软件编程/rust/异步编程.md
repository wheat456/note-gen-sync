![[Pasted image 20241229230108.png]]
- tokio 执行的是任务，任务就是future 和waker 等组成的结构体，理论上执行器要轮流执行多个任务，这些任务在一个队列里，当执行其中一个任务时，会poll任务中的future,而future又含有async的逻辑代码，也就是去执行这些逻辑代码，当执行阻塞时，可以返回pending,那么执行器收到pending后就，就去执行那些能被poll的任务。同时，如果一个任务的等待的东西好了，就会让反应器去调用waker,之后该任务的future又能被poll了