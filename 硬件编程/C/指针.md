```
数据类型 *指针变量名;
```
这里的 `*` 是指针声明符，表示该变量存储的是一个内存地址，而不是具体的数据值；前面的数据类型决定了指针指向的内存如何被解析

```c
int a = 10;
int *p = &a; // 定义一个整型指针 p，并将其指向变量 a 的地址

*p = 20;     // 修改 p 指向地址中的内容，此时 a 的值变为 20
printf("%d\n", *p); // 输出 20
```