# 成员变量 (Fields)
1. 类内部的私有数据存储，通常以 `_camelCase` 命名。
2. 使用 `const` 或 `readonly` 修饰的基础配置。
3. 永远不要将成员变量设为 `public`。
# 属性 (Properties)
对外暴露的逻辑接口，包含 `get` 和 `set` 访问器
任何需要让外部类访问的成员。


属性名和成员变量的名字一样，大小写不一样
\_hello ,Hello

这里需要首字母大写
```cs
        [RelayCommand] 
        public void AddCount()
        {
            Count++;
        }
```



手动实现属性变化通知
```cs
public class ViewModel : INotifyPropertyChanged  
{  
private int _count;  
  
public int Count  
{  
get => _count;  
set  
{  
if (_count != value)  
{  
_count = value;  
OnPropertyChanged(nameof(Count));  
}  
}  
}  
  
public event PropertyChangedEventHandler PropertyChanged;  
  
protected virtual void OnPropertyChanged(string propertyName)  
{  
PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(propertyName));  
}  
}
```

**`ref`** 是 C# 中的关键字，它用于指示该参数是**按引用传递**的。这意味着传递给 `SetProperty` 的 **`field`** 参数（在你调用时 `_count`）会被**修改**，而且修改会直接反映到调用者。



在使用mvvm 后属性变化的写法,这个属性是双向的，但是在前端通过mode=twoway来改变
```cs
        private int _count;

        public int Count
        {
            get { return _count; }
            set
            {
                SetProperty(ref _count, value);
            }
        }
```



# 控件
创建一个Controls的一个文件夹
在里面创建自己的控件
然后再父组件中调用
`xmlns:controls="clr-namespace:MyApp.Controls"`
<controls:MyDataCard />

如果子组件需要接受参数，就
`<controls:MyDataCard DataContext="{Binding SelectedUser}" />`
或者在组件的cs文件中定义依赖属性

propdp 依赖属性
propfull 手动控制逻辑属性
cw打印
ctor构造函数
inst经典类型检查


vue tailwind 隐藏scroll-bar
安装
```
tailwind-scrollbar-hide
```
在style.css中引入它
然后在class中使用`scrollbar-hide`


# c# 
## timer
```cs
var atimer = new System.Timers.Timer(1000); //使用这个timer
 
atimer.Elapsed += (sender, e) => { 

}

timer.AutoReset = true; 
timer.Enabled = true;
```

异步timer
- 主要用于循环
```cs
using var atimeer = new PeriodicTimer(TimeSpan.FromSeconds(1));
while (await timer.WaitForNextTickAsync()) { // 这里写你的逻辑 await DoWorkAsync(); }
```

执行一次
`await Task.Delay(TimeSpan.FromSeconds(1));`

wpf中,这个是ui上面的timer,如果长时间在里面ui就会卡顿
```cs
DispatcherTimer timer = new DispatcherTimer();
timer.Interval = TimeSpan.FromSeconds(1);
timer.Tick += (s, e) => {
    myLabel.Content = DateTime.Now.ToString(); // 直接更新UI，安全！
};
timer.Start();
```

## 存储
Dictionary 用于kv


# webview
应该要在前端通过xmlns引入webview
然后在后端代码中
```cs
private async void InitializeWebView()
    {
        // 1. 等待初始化完成
        await myWebView.EnsureCoreWebView2Async();

        // 2. 此时 CoreWebView2 属性才可用，可以进行配置
        myWebView.CoreWebView2.Settings.IsPasswordAutofillEnabled = false;

        // 3. 导航到目标页面
        myWebView.Source = new Uri("https://www.google.com");
    }
```
 `await myWebView.EnsureCoreWebView2Async();` 异步初始化webview
 `myWebView.Source = new Uri("https://www.google.com");` 导航到一个网站

如果想加载自己本地的网页，首先就应该找到本地的路径
`string distPath = System.IO.Path.Combine(AppDomain.CurrentDomain.BaseDirectory, "web_dist");`
然后就将路径与自定义域名绑定
```cs
webView.CoreWebView2.SetVirtualHostNameToFolderMapping( "myapp.local", distPath, CoreWebView2HostResourceAccessKind.Allow );
```
`webView.Source = new Uri("https://myapp.local/index.html");`
然后访问自定义域名

# system.text
```cs
byte[] utf8Bytes = Encoding.UTF8.GetBytes(message); Console.WriteLine($"UTF-8 字节长度: {utf8Bytes.Length}");
string decodedMessage = Encoding.UTF8.GetString(utf8Bytes);
```

字符串拼接
```cs
var sb = new StringBuilder()
sb.append("xx")
sb.Replace("xx","1")
sb.tostring()
```

## json
```cs
var user=new {xxxx} //一个json对象
string jsonstr=JsonSerializer.Serialize(user); //json转str
var restoredUser = JsonSerializer.Deserialize<dynamic>(jsonString);
```

# system.io.path
```cs
string fullPath = Path.Combine(folder, fileName);
```

获取文件名
```cs 
Path.GetFileName(path)
Path.GetFileNameWithoutExtension(path) //不带后缀
- **获取扩展名**：`Path.GetExtension(path)` -> `".jpg"`
    
- **获取目录**：`Path.GetDirectoryName(path)` -> `"C:\Users\Admin\Desktop"`
  Path.ChangeExtension 替换后缀
  string newFile = Path.ChangeExtension(oldFile, ".mp3");
```
Path.GetRandomFileName() 生成随即名
Path.GetTempPath() 获取零时文件路径