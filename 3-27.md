# 成员变量 (Fields)
1. 类内部的私有数据存储，通常以 `_camelCase` 命名。
2. 使用 `const` 或 `readonly` 修饰的基础配置。
3. 永远不要将成员变量设为 `public`。
# 属性 (Properties)
对外暴露的逻辑接口，包含 `get` 和 `set` 访问器
任何需要让外部类访问的成员。


属性名和成员变量的名字一样，大小写不一样
\_hello ,Hello

这里需要首字母大写
```cs
        [RelayCommand] 
        public void AddCount()
        {
            Count++;
        }
```



手动实现属性变化通知
```cs
public class ViewModel : INotifyPropertyChanged  
{  
private int _count;  
  
public int Count  
{  
get => _count;  
set  
{  
if (_count != value)  
{  
_count = value;  
OnPropertyChanged(nameof(Count));  
}  
}  
}  
  
public event PropertyChangedEventHandler PropertyChanged;  
  
protected virtual void OnPropertyChanged(string propertyName)  
{  
PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(propertyName));  
}  
}
```

**`ref`** 是 C# 中的关键字，它用于指示该参数是**按引用传递**的。这意味着传递给 `SetProperty` 的 **`field`** 参数（在你调用时 `_count`）会被**修改**，而且修改会直接反映到调用者。



在使用mvvm 后属性变化的写法,这个属性是双向的，但是在前端通过mode=twoway来改变
```cs
        private int _count;

        public int Count
        {
            get { return _count; }
            set
            {
                SetProperty(ref _count, value);
            }
        }
```



# 控件
创建一个Controls的一个文件夹
在里面创建自己的控件
然后再父组件中调用
`xmlns:controls="clr-namespace:MyApp.Controls"`
<controls:MyDataCard />

如果子组件需要接受参数，就
`<controls:MyDataCard DataContext="{Binding SelectedUser}" />`
或者在组件的cs文件中定义依赖属性