# 1-3
MG82F6D32AY48：
- **ADx**：ADC
- **AINx**： 只能作为adc来用
- **BMxOUT**：**BMxOUT**（通常指 **OBM**，即 Output Break & Modulator）是一个非常实用且高级的混合信号控制功能。
- **T0COB**：定时器溢出方波输出
- **PWMxA|B**：WM0A/PWM0B 属于 PWM0 模块），**共享同一套频率和周期参数**,输出pwm
- **CEX** ：引脚主要用于 **PCA (Programmable Counter Array，可编程计数器阵列)** 模块。输入捕获
- **ACxout**：模拟比较输出
## 准备
spi flash W25Q32

# 1-4
shift+D:抓取某个点进行移动
