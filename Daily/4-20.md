在逆变和协变中，out 关键字会限制修饰的变量，必须出现在返回的位置，例如函数的返回
in 关键字会强制变量在输入的位置，例如函数的参数

在协变（out）中，将一个`IEnumerable<Animal>=List<Dog>`实际上是让右侧的对象，呈现某种接口的形式，该接口限定了只能将变量out

同理，`IComparer<Animal> animalComparer = new AnimalWeightComparer(); IComparer<Dog> dogComparer = animalComparer;`