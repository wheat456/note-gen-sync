readonly 是修饰字段的，表示其不能在被赋值


# 创建型模式
## 工厂模式
- **简单工厂：** 负责生产“某一种”产品（通过参数决定哪种）。
	- 就是一个工厂类可以通过条件返回不同的对象
- **工厂方法：** 每一类产品由一个专门的子工厂生产。
    - 继承一个xx工厂基类，继承玩具工厂的基类能创建玩具厂，继承手机工厂的基类能创建手机厂等，里面有对应的类的创建函数
- **抽象工厂：** 负责生产“一整套”相关的产品系列。
	- 继承一个工厂基类/接口，里面有各种东西的生成方法，但是都是指的的产品的共同基类/接口。比如手机接口，电脑接口，不同的工厂创建的具体手机和电脑是不同的，但是都是继承的是手机接口和电脑接口
### 工厂基类
- 拥有创建方法，可以拥有多个产品的创建方法，比如创建A产品方法，创建B产品方法
```cs
public abstract void createProduct();
```
### 实际工厂
- 继承基类，实现创建函数
```cs
```


### 产品基类

### 实际产品 
- 继承产品基类



# 结构形模式

## 装饰器模式
- 在每一个函数中可以去调用基类中同样的函数

1. 有两个角色，一个是`被装饰对象`，另一个是`装饰器`
2. `被装饰对象`继承一个`基类/接口`
```cs
interface IBasic{
 void func1(){
 }
 void func2(){
 }
....
}



class BasicClass:IBasic{
	//实现接口中的方法
}
```
3. `装饰器基类`也同样实现其`接口`，但是内部还要添加一个`字段`，类型为继承的`接口/基类`
```cs
abstract class BasicDecorator(IBasic BeDecoratedClass) :IBasic{
	//实现接口,并表明虚方法
	...
	
	// 声明一个接口类型的字段
	IBasic BeDecoratedClass;

}

```
4. 实际装饰器继承装饰器基类，并重写装饰器基类中的虚方法
```cs
Class ConcreteDecorator(IBasic xxx):BasicDecorator(xxx){
	// 从写方法
	
	//
	IBasic BeDecoratedClass=xxx;
}

```
5. 最终，可以实例化一个`装饰器`，并填入需要`被装饰的类`，同样也可以填入`装饰器`
```cs


IBaisc Aa= new ConcreteDecorator(new BasicClass){}

```

##  适配器模式
- **目的**：让两个接口兼容，比如想要A,但是有B,那么通过适配器将B转换为A类型
	- 也就是说包装调用对象种的方法，本来返回的是`int`,但是可以调用`被包装对象`的方法后，将结果修改以适配实际的需求
- 创建一个适配器，这个适配器可以继承某种`接口或基类`
```cs
//传入源，也就是需要被适配的对象
class Adapter(Source object1){
//定义行为
data GetData(){

object1.getdate
//再对数据进行一些修饰，就可以返回了
	}
}

//使用适配器,该方法返回的是自己想要的数据类型，所以完成了包裹
adapter.GetData() 
```

## 代理模式


- 目的：给目标对象添加一层代理，用于限制对其的访问
- 不同代理的区别是，各种代理的职责不同

1. 创建一个`接口/类`，`代理`和`真实对象`都继承这个
2. 其中`代理`持有一个`真实对象`的字段
3. 可以在代理的操作方法中添加一些行为，从而实现各种功能，在访问真实对象之前

```cs
interface Subject{
	void Operation(){
	
	}
}
class proxy:Subject{
	RealSubject _realsubject;
	void Operation(){
	}
}

class RealSubject:Subject{
	void Operation(){
	
	}
}

```
### 远程代理
- 建立一个本地对象，这个对象是远程对象的一个映射

### 虚拟代理
- 在真正使用某个对象之前，先建立一个它的虚拟对象，这个虚拟对象就代表虚拟代理
- 延迟加载
### 保护代理
- 对目标对象进行限制
### 智能引用代理
- 建立一个代理，管理多个对象对同一个对象的访问

### 缓存代理
- 缓存访问的结果

## 组合模式

- **目的**：让客户端对“单个对象”（叶子）和“对象容器”（组合）的使用具有**一致性**
	- 也就是说调用单个对象使用的方法于调用组合种的的方法都是一样的，不过组合是递归的调用集合种的方法
- 在组合模式中有两种角色，一种是`叶子`也就是说它没有持有`叶子集合`，另一种是`组合`，也就是说它持有`叶子集合`或者`组合集合`
- 无论是`叶子`还是`组合`都继承同一个`基类`，拥有同一种操作方法，但是`组合`还拥有`添加`和`移除`等操作`集合`的方法

```cs
abstract class BaseClass{
	// 操作方法，这个方法对于叶子和组合都有,都应该实现
	abstract void Operation(){
	
	}
	
	//组合才有这个方法，用于操作集合
	virsual void add(){
	
	}
}

//创建一个叶子类型，没有集合
class leaf:BaseClass{
	void Operation(){
	
	}
}

class composite:BaseClass{
	
	// 这里代表这个集合既可以装叶子，也可以装自身类型
	list<BaseClass> 
	

	void Operation(){
		//这里可以遍历集合，调用他们的操作方法
	}
	// 添加集合方法
	override void add(){
	
	} 
	
	
}

```



# 行为模式

## 迭代器
- 拥有一个集合的`接口/基类`，拥有一个迭代器的`接口/基类`
- `集合`中可以创建`迭代器`，迭代器拥有，`HasNext()`方法，表示还有下一个吗，`next()`方法，表示返回下一个元素，`reset()`方法代表重置
```cs
// 聚合体/集合 理论上需要继承一个接口来解耦，但是这里就省略了


Class Aggragate{
	Iterator CreateIterator(){
		
	}
}

// 理论上要继承一个接口，但是这里省略了
// 传入对象,得到相关上下文
// 如果需要进行对象处理，那么这段逻辑应该封装在对象中，而不是在迭代器中，而迭代通过对象访问那些方法
Class Iterator(Aggragate list){
	data OnNext(){
		
	}
	
	bool HasNext( ){
		
	}
	
	void Set(){
		
	}
}


// 获取迭代器
new Aggragate().CreateIterator()
// 询问是否有元素
iterator.HasNext()
// 如果有,调用OnNext()拿到数据
iterator.OnNext() 

```


## 状态模式
- 有两个角色，一个是状态，一个是对象
- 首先一个对象拥有改变状态的方法，拥有一些对外暴露的行为，持有状态字段，但是对外暴露的行为实际上是取决于状态的，也就是具体是由状态去实现到底要干什么
- 状态可以通过持有对象来改变状态，状态中的方法是实际上在该状态下会执行的特定的逻辑


1. 创建一个类，拥有一个为`状态`的字段，同时这个类也应该有一些行为方法
```cs
// 假如创建一个“Person”的类
Class Person{
	// 拥有一个状态的字段
	State state;
	
	// 各种行为方法,里面通过状态去调用
	void eat(){
		state.HandleEate(this);
	}
	
	void sleep(){
		state.HandleSleep(this);
	}
	
	// 切换状态的方法
	void SetState(State state){
		_state=state
	}
	
}
```

2. 创建`状态`的基类，同时拥有在该状态下处理各种行为的方法
```cs
interface IBaseState{
//持有人的本身，方便拿到一些上下文信息
	void HandleEat(Person person){
		
	}
	void HandleSleep(Person person){
	
	}
	
}

```
3. 创建真正的状态，继承至状态基类
```cs
// 假如有两个状态，分别是正常状态和生病状态

class NormState:IBaseState{
	// 实现接口中的方法，定义自己的逻辑
	void HandleEat(Person person){
		//吃的多，然后生病了
		person.SetSate(new SickState())
		
	}
	void HandleSleep(Person person){
		//着凉了，然后生病了
		person.SetSate(new SickState())
	}
	

}

class SickState:IBaseState{
	// 实现接口中的方法，定义自己的逻辑
	void HandleEat(Person person){
		//吃的多，大补，然后好了
		person.SetSate(new NormState())
		
	}
	void HandleSleep(Person person){
		//睡一觉，然后好了
		person.SetSate(new NormState())
	}

}

```


## 观察者模式

### 可观察对象
### 通用接口
- 拥有添加`观察者`的函数
```cs 
void Attach(IObservable){
}
```
- 拥有去除`观察者`的函数
```cs
void Dettach(IObservable){
}
```
- 拥有`通知`的函数
```cs 
void Notify(){

}
```
### 具体可观察者
- 实现上述的接口
- 拥有一个观察者对象的`List`
```cs
List<Observaber>
```
- 在`通知函数`中调用每个`观察者`的`更新函数`
```cs
void Notify(){
	xxxObservaber.Update();
}
```
- 拥有一个`获取数据方法`，给`观察者`用
### 观察者
### 通用接口
- 拥有`更新`的函数
```cs
void Update(){

}
```
### 具体观察者
- 拥有可观察者的引用
```cs
xxx(IObservable xx){
}
```
- 在`更新函数`中去调用`可观察者`的`获取数据方法`
```cs
void Update(){
	IObservable.GetData();
}
```

## 中介者模式
1. 顾名思义，需要一个`中介者`，该`中介者`需要持有`需要通知的对象`，同时提供一个`方法`给`客户`调用，`客户`通过这个`方法`来告知`中介者`相关的事情
```cs
Class Mediator{
	// 假如这里有2个客户
	Client client1;
	Client client2;
	
	// 这是一个通知方法，但是由客户来调用的，意思是客户想要通知中介者什么
	// 当然可以命名为“来之客户的消息”这个函数，其中p1 代表着是哪一个客户，以及p2代表着想要传达的东西
	void Notify(p1，p2){
		// 在这里可以拿到客户传递的东西，可以进行一些操作，比如去通知另一个客户，通过调用它暴露的接口
	}
	// 在构造中给客户设置中介者
	Mediator(client1,client2){
		client1.setxx
		client2.setxx
	}
	
}
```

2. `客户`需要持有`中介者`引用，并提供一个设置`中介者`的`方法`，可能会想直接通过构造传入中介者就可以，但是从代码可读上面，还是让中介者调用设置方法，这样中介者知道需要去通知那些客户。`客户`也应该提供一些方法给`中介者`，以便`中介者`接受其他`客户`的消息时，能知道如何通知这个`客户`

```cs
class Client{
	Mediator med;
	
	//设置中介者的方法
	void SetMed(){
	}
	// 这个函数是提供给中介者的，意思是让中介者在想要告诉我什么时，就调用这个方法
	void OtherWantTellMe(){
	}
	
	// 这里可以调用中介者的通知函数，去告诉中介者一些事情
	void Operation(){
		med.Notify()
	}

}
```

3.  实例化
```cs

Client client1 = new();
Client client2 =new();
Mediator med = new(client1,client2);

// 这里去告诉中介者一些事情，同理client1也调用该方法去告诉中介者
client2.Operation()


```