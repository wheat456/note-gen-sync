# 重定向

在usart.c文件下包含`#include<stdio.h>`

同时加上

```c
int _write(int file, char *ptr, int len)
{
    HAL_UART_Transmit(&huart1, (uint8_t *)ptr, len, HAL_MAX_DELAY);
    return len;
}
```

记得使用换行符才有有效输出

推荐：加换行符 `\r\n`
