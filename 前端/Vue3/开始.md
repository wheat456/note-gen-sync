创建项目 
`npm create vite@latest`
`npm install` 安装包
`npm run dev`
# 导入
```ts
export const xxx{

}
```
在其他文件导入时，需要使用`{}`
```ts
import {} ....
```

如果导出时用了`default`
```ts
export default const xxx{

}
```
其他地方就可以
```ts
import xxx
```