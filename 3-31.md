PART_ 是一个特定的名字，可以让后端代码找到对应的组件
IsTabStop="False" 按下tab时，无法聚焦到这个组件

ContentTemplate 就算button 等组件的content属性接受的类型
```xml
<Button>
    <Button.ContentTemplate>
        <DataTemplate>
            </DataTemplate>
    </Button.ContentTemplate>
</Button>
```
`ContentTemplate` 的内部必须是一个 **`<DataTemplate>`**，而在 `DataTemplate` 里面，你可以放几乎任何视觉元素。

Content="{Binding SelectedUser}" 可以绑定数据，但是如果没有设置数据模板的话，就只会显示类名，所以要将数据类型与数据模板关联

contentTemplate 可以定义在行内定义，也可以在resources中定义。如果在行内定义就可以直接在datatemplate 中绑定数据。
如果在resource中定义，那么分两种情况
1. 如果datatemplate没有指定类型，那么这个数据模板就不知道应用到那个类型上，就需要以静态资源的方式引入
```xml
<Button Content="{Binding SomeUserObject}" ContentTemplate="{StaticResource MyUserTemplate}" />
```
2. 如果datatemplate 指定了类型
```xml
<Window.Resources>
    <DataTemplate DataType="{x:Type local:User}">
        <StackPanel>
            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
            <TextBlock Text="{Binding Status}" />
        </StackPanel>
    </DataTemplate>
</Window.Resources>

<Button Content="{Binding SomeUserObject}" /> 
<ContentControl Content="{Binding AnotherUser}" />
```


标记扩展，就是conten={}等什么什么的={}，这个{}就是标记扩展