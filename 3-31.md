PART_ 是一个特定的名字，可以让后端代码找到对应的组件
IsTabStop="False" 按下tab时，无法聚焦到这个组件

ContentTemplate 就算button 等组件的content属性接受的类型
```xml
<Button>
    <Button.ContentTemplate>
        <DataTemplate>
            </DataTemplate>
    </Button.ContentTemplate>
</Button>
```
`ContentTemplate` 的内部必须是一个 **`<DataTemplate>`**，而在 `DataTemplate` 里面，你可以放几乎任何视觉元素。