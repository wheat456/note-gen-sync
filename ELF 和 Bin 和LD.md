
ELF 文件带有一些备注信息


### 1. 中断向量表（Vector Table）

通常位于 `.bin` 文件的**最开头（偏移量 0x00000000）**。CPU 上电复位的第一瞬间就会读取这块区域：
- **栈顶指针初始值（Initial MSP）**：告诉 CPU 运行栈放在内存（SRAM）的哪个最高地址。
- **复位中断入口（Reset Handler 地址）**：CPU 启动后跳转执行的第一行代码地址。
- **异常与外设中断入口**：NMI、HardFault、SysTick 以及各硬件外设（UART、TIM 等）中断服务函数（ISR）的跳转地址清单。 
### 2. 代码段（`.text`）

紧接在向量表之后，是整个固件的核心主体：
- 所有的 C/C++ 业务逻辑、库函数编译后生成的**纯机器指令**（操作码 Opcode）。
- 芯片上电后，就是按这里的指令逐条读取执行的。
### 3. 只读数据段（`.rodata`）

存放程序运行期间**只读且不可修改**的常量数据，通常紧随代码段存放，包含：

- 程序中写死的字符串常量（如 `"System Start\r\n"`）。 
- 查表法使用的常量表（如正弦波查找表、字体点阵库、CRC 校验表）。
- 用 `const` 修饰的全局变量。

### 4. 数据段的初始值镜像（`.data` 的加载视图 / LMA）

这是很多人容易混淆的地方：
- 程序中带有初始值的全局变量（如 `int speed = 100;`）在运行时必须放在 RAM 里以便修改。
- 但单片机掉电后 RAM 数据会丢失，因此**这些初值必须在 Flash（即 `.bin` 文件）中保留一份备份**。
- 芯片复位时，启动代码会执行一段循环，把 `.bin` 中这一段数据原封不动地“搬运（Copy）”到内存 SRAM 的指定区域。

### 两个重要补充：`.bin` 里“有什么”和“没有什么”

- **文件里没有 `.bss`**：未初始化的全局变量（如 `int buffer[1024];`）**完全不会打包进 `.bin` 文件**。`.bin` 只包含启动代码，启动代码在上电时会直接把 RAM 里对应的区域用循环清零。
    
- **间隙填充区（Padding）**：如果代码中通过链接脚本（Linker Script）强行把某个函数或数据段对齐到特定物理地址（比如相隔 1KB），中间的空隙会被编译器强行塞入一连串的 `0x00` 或 `0xFF`，保证文件偏移与内存物理地址能够 1:1 严格对齐。


# ld 文件

这块芯片有哪些物理存储器（Flash、SRAM 大小及起始地址）、每个代码和数据段该塞到哪块区域、栈顶和堆底又该画在什么边界。

```
/* 1. 指定程序入口函数 */
ENTRY(Reset_Handler)

/* 2. 声明硬件的物理内存分布 */
MEMORY
{
  FLASH (rx)  : ORIGIN = 0x08000000, LENGTH = 512K
  RAM   (rwx) : ORIGIN = 0x20000000, LENGTH = 128K
}

/* 3. 节区映射规则 */
SECTIONS
{
  /* 中断向量表与只读代码存入 FLASH */
  .text :
  {
    KEEP(*(.isr_vector))    /* 强制保留中断向量表，防止被优化删除 */
    *(.text*)               /* 收集所有 .o 文件中的指令代码 */
    *(.rodata*)             /* 收集所有常量数据 */
  } > FLASH

  /* 已初始化的全局变量：Flash 存储初值，RAM 中运行 */
  .data :
  {
    _sdata = .;             /* 标记 data 段在 RAM 中的起始位置 */
    *(.data*)
    _edata = .;             /* 标记 data 段在 RAM 中的结束位置 */
  } > RAM AT> FLASH         /* VMA 在 RAM，LMA 在 FLASH */

  _sidata = LOADADDR(.data); /* 获取 .data 在 Flash 中的物理备份首地址 */

  /* 未初始化的全局变量：仅在 RAM 中分配空间 */
  .bss :
  {
    _sbss = .;              /* 标记 bss 起始 */
    *(.bss*)
    _ebss = .;              /* 标记 bss 结束 */
  } > RAM
}
```