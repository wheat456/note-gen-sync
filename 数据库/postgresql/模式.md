模式相当于分类标签，默认的表都在public 的模式下
模式可以包含视图、索引、数据类型、函数和操作符等
## 创建模式
```sql
CREATE SCHEMA myschema;
```
### 在模式下创建表
```sql
CREATE TABLE my_schema.users ( // my_schema是模式
    id SERIAL PRIMARY KEY,
    name TEXT NOT NULL
);
```
## 删除模式
`DROP SCHEMA myschema;`
`DROP SCHEMA myschema CASCADE;` 删除所有东西
``